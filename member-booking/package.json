{
  "name": "@alchemy/member-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Alchemy East Fremantle member booking channel: API layer, waiver service, door list and reconciliation export.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "netlify dev",
    "check": "npm run typecheck && npm run test"
  },
  "dependencies": {
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.1",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
